{
  "name": "pms-voice-tool-integrated",
  "private": true,
  "version": "0.2.0",
  "description": "Integrated Mazik PMS and Purchase copilot monorepo.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace @pms-voice/api",
    "dev:web": "npm run dev --workspace @pms-voice/web",
    "build": "npm run build --workspaces --if-present",
    "check": "npm run check --workspaces --if-present"
  }
}
